<Window x:Class="IndeterminateProgressBarWithNinjectDI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Indeterminate Progress Bar Example" Height="85.294" Width="525"
        DataContext="{Binding Path=MainWindowViewModel, Source={StaticResource ServiceLocator}}">

    <Grid Background="Silver">
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="10, 15, 0, 0">
            <Label Content="Process state:"/>
            <Label Content="{Binding ProcessStatus}"
                   Width="240"/>
            <Button Content="Start Process"
                    Width="90"
                    Margin="70,0,0,0"
                    Command="{Binding StartBtnClick}"/>
        </StackPanel>
    </Grid>
</Window>
